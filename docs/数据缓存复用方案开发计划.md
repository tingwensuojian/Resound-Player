# 数据缓存复用方案开发计划

## 选型决策

**最终方案：方案 B（SWR + useApiData 组合式）+ 平台适配**

- 长期最优，与项目现有 `reactive()` + composable 模式一致
- 支持 Web / Electron 桌面双平台差异化适配
- 可增量迁移，与现有代码兼容

## 总体路线（4 个阶段）

```
阶段 1 ── 基础设施层（3 个文件）          ✅ 已完成
阶段 2 ── 高价值页面迁移（5 个页面）      ✅ 已完成
阶段 3 ── 其余页面迁移（按需）            ✅ 已完成
阶段 4 ── 桌面端增强（持久化缓存）        ✅ 已完成
```

## 当前完成状态（2025.05.12）

| 阶段 | 文件 | 状态 |
|---|---|---|
| **P1** | `src/utils/requestDedup.ts` | ✅ |
| **P1** | `src/stores/apiCache.ts` | ✅ |
| **P1** | `src/composables/useApiData.ts` | ✅ |
| **P1** | `src/stores/user.ts` 缓存生命周期 | ✅ |
| **P1** | `src/App.vue` 初始化 | ✅ |
| **P2** | `PlaylistDetailPage.vue` | ✅ |
| **P2** | `AlbumDetailPage.vue` | ✅ |
| **P2** | `ArtistDetailPage.vue` | ✅ |
| **P2** | `PlaylistPanel.vue` | ✅ |
| **P2** | `RankPanel.vue` | ✅ |
| **P3** | `MvPanel.vue` | ✅ |
| **P3** | `PodcastDetailPage.vue`（数据来自 props，父级已覆盖） | ✅ |
| **P3** | `LanguageDetailPage.vue` | ✅ |
| **P3** | `HomePanel.vue` | ✅ 每日推荐/雷达已加缓存 daily TTL |
| **P3** | `SearchPanel.vue`（不缓存） | ✅ 无需改动 |
| **P3** | `DailyRecommendPage.vue`（不缓存） | ✅ 无需改动 |
| **P4** | `electron/main.js` IPC 缓存处理 | ✅ |
| **P4** | `electron/preload.js` 暴露 cacheApi | ✅ |
| **P4** | `src/types/global.d.ts` 桌面端声明 | ✅ |
| **P4** | `global.d.ts` Vue SFC 模块声明 | ✅ |
| **P4** | `src/stores/apiCache.ts` ElectronFileBackend | ✅ |
| **P4** | `src/stores/apiCache.ts` 自动检测平台 | ✅ |

---

## 阶段 1：基础设施

### 1.1 `src/utils/requestDedup.ts` — 请求去重

- 基于 Promise Map，相同 key 同时 pending 时复用 Promise
- 请求完成后立即释放 Map entry
- 生命周期短，只在请求期间生效

### 1.2 `src/stores/apiCache.ts` — 缓存管理器

- 全局 `reactive()` 单例
- 内存 Map + LRU 淘汰 + TTL 检查
- 缓存分组配置：

| 分组 | 示例 key | TTL | 最大条数 |
|---|---|---|---|
| 实体 | `song:${id}`, `album:${id}`, `artist:${id}` | 30min | 500/200/200 |
| 列表 | `playlist:${id}`, `toplist`, `highquality` | 10min / 5min | 200/50/50 |
| 不缓存 | `search:`, 推荐类 | 0 | 0 |

- 用户 ID 隔离：key 自动追加 `@${userId}`（仅对含身份字段的 API）
- 暴露方法：`getCache`, `setCache`, `hasCache`, `clearAll`, `clearUserScopedCache`
- 桌面端预留后端接口 `setBackend(backend)`

### 1.3 `src/composables/useApiData.ts` — SWR 组合式

API：

```ts
function useApiData<T>(
  cacheKey: string | (() => string),
  fetcher: () => Promise<T>,
  options?: {
    ttl?: number
    dedupMs?: number
    enabled?: boolean | Ref<boolean>
    immutable?: boolean
    onSuccess?: (data: T) => void
    onError?: (err: Error) => void
  }
): {
  data: Ref<T | null>
  loading: Ref<boolean>
  error: Ref<string>
  refresh: () => Promise<void>
  mutate: (data: T) => void
}
```

行为：
1. 构造 `cacheKey`（追加 `@${userId}` 如需要）
2. 检查 dedupMap → 相同 key pending → 复用 Promise
3. 检查 apiCache → 命中且未过期 → `immutable?` 直接返回 : data+后台静默刷新
4. 未命中 → 发起请求 → 写入缓存 → 返回数据

**重要**：身份相关字段（liked, subscribed）不在缓存中，必须从 `userStore` 读取

### 1.4 用户态修改

`src/stores/user.ts`：
- `logout()` 中新增 `clearApiCache()`
- `loginWithCookie/restoreUidLogin` 成功后调用 `clearApiCache()`
- 不清 `unblock-cache`（音源结果与用户无关）

---

## 阶段 2：高价值页面迁移

按优先级排列，每次迁移一个页面。

### 2.1 `PlaylistDetailPage.vue`
- cache key: `playlist:${id}`
- TTL: 10min（实体类）
- 注意：`subscribed` 状态不走缓存，走 `useEntitySubscribe`

### 2.2 `AlbumDetailPage.vue`
- cache key: `album:${id}`
- TTL: 30min

### 2.3 `ArtistDetailPage.vue`
- cache keys: `artist:${id}` (30min), `artist:${id}:hotsongs` (10min), `artist:${id}:albums` (10min)

### 2.4 `PlaylistPanel.vue`
- cache keys: `playlist:cats` (10min), `playlist:list:${cat}:${order}:${page}` (5min), `playlist:highquality:${cat}` (5min)

### 2.5 `RankPanel.vue`
- cache key: `toplist:detail` (5min)

---

## 阶段 3：其余页面迁移（按需）

| 页面 | Cache key | TTL | 优先级 |
|---|---|---|---|
| `MvPanel.vue` | `mv:list:${area}:${type}:${order}:${page}` | 5min | 中 |
| `PodcastDetailPage.vue` | `podcast:${id}` | 10min | 中 |
| `LanguageDetailPage.vue` | `playlist:${id}` | 10min | 中 |
| `HomePanel.vue` | `daily:songs` / `daily:playlists` | 到次日 6:00 失效 | 中 |
| `HomePanel.vue`（私人 FM） | 不缓存 | 每次刷新更新 | 低 |
| `SearchPanel.vue` | `search:${keyword}:${type}` | 不缓存 | 低 |
| `DailyRecommendPage.vue` | 每日推荐 | 不缓存 | 低 |

---

## 数据缓存策略速查：按数据更新频率

### 每日更新数据（每日推荐、私人雷达）

适用 `dailyTtl()` 辅助函数（计算到次日 6:00 的毫秒数）：

```ts
function dailyTtl(): number {
  const now = Date.now();
  const next = new Date();
  next.setDate(next.getDate() + 1);
  next.setHours(6, 0, 0, 0);
  return Math.max(next.getTime() - now, 60 * 60 * 1000);
}

// 使用方式（在 fetch 函数中）：
const cached = apiCache.get('daily:songs');
if (cached?.data) {
  // 直接使用缓存数据，跳过 API
  return;
}
// 请求成功后：
apiCache.set('daily:songs', data, dailyTtl());
```

### 每次刷新更新数据（私人 FM）

**不缓存**。每次组件挂载/刷新时重新请求，不检查 apiCache，也不写入。

```ts
// 直接请求，不走缓存
const { data } = await getPersonalFm(userStore.loginCookie || undefined);
```

### 缓存决策表

| 数据特征 | 策略 | 示例 |
|---|---|---|
| 每日更新一次（6:00） | `dailyTtl()`，缓存到次日 6:00 失效 | 每日推荐、私人雷达 |
| 用户操作变更 | 不缓存实体状态，从 `userStore` 读取 | 收藏/订阅/关注状态 |
| 每次刷新需最新 | 不缓存，每次直接请求 | 私人 FM |
| 极少变更 | 长 TTL（10-30min） | 歌曲/专辑/歌手详情 |
| 中等变更频率 | 短 TTL（3-5min） | 歌单列表、排行榜 |

---

## 阶段 4：桌面端增强（Electron 持久化）

### 4.1 CacheBackend 接口抽像

```ts
interface CacheBackend {
  get(key: string): CacheEntry | null
  set(key: string, entry: CacheEntry): void
  delete(key: string): void
  clear(): void
}
```

Web 端使用 `MemoryBackend`，桌面端使用 `ElectronFileBackend`。

### 4.2 ElectronFileBackend

- 内存 Map + 文件持久化（userData 目录下 `api-cache.json`）
- 应用启动时加载文件到内存
- 写入防抖 2s 合并多次写入
- `flushPending()` 在退出前触发一次立即刷写（`beforeunload` 时 best-effort 调用，用于缩短数据丢失窗口）
- 文件过大时自动裁减：
  Electron 文件后端保留最近 300 条；Web localStorage 回退到最近 100 条

### 4.2.1 LRU 淘汰机制

- `CacheEntry` 新增 `lastAccessed` 字段，每次 `get()` 时更新
- `_runCleanup()` 按缓存族分别执行 LRU，按 `lastAccessed` 从旧到新排序后淘汰最久未访问的条目
- 旧数据兼容：缺失 `lastAccessed` 时回退到 `timestamp`
- `CACHE_MAX` 各分组独立上限：

| 分组 | 最大条数 |
|------|---------|
| song / album / artist / podcast | 500 / 200 / 200 / 200 |
| playlist:list / playlist / lang:playlist / daily / radar | 200 |
| playlist:highquality / toplist / mv:list | 50 |
| 其他未单独归类分组 | 200 |

### 4.2.2 选择性清除

- `clearUserScoped()` 只清除含 `@userId` 后缀的缓存（登出时调用）
- 公共缓存（榜单、热门等）不受影响

### 4.3 preload 暴露 + platform 模块

- `electron/preload.js` 暴露 `cacheApi` IPC 通道
- `src/types/global.d.ts` 显式补充 `window.appEnv.cacheApi` 桌面端声明
- `global.d.ts` 负责 `*.vue` 模块声明
- `src/utils/platform.ts` 继续负责桌面能力判断；缓存后端实例由 `apiCache.init()` 内部选择

### 4.4 配置注入

```ts
// App 启动时
apiCache.init()

// apiCache 内部
this._backend = platform.isDesktop
  ? new ElectronFileBackend()
  : new MemoryBackend()
```

---

## 平台差异汇总

| 特性 | Web | Electron 桌面 |
|---|---|---|
| 缓存后端 | 纯内存 Map | 内存 + 文件持久化 |
| 实体 TTL | 30min | 30min（无差异） |
| 列表 TTL | 5-10min（按 `LIST_VOLATILE` / `LIST`） | 5-10min（与 Web 相同） |
| 最大条数 | 按分组上限（CACHE_MAX） | 按分组上限（CACHE_MAX） |
| LRU 淘汰 | ✅ lastAccessed | ✅ lastAccessed |
| 持久化 | 无 | 支持（防抖 2s + 退出前 best-effort flushPending） |
| 冷启动缓存 | 无 | 从文件恢复 |
| 登出清理 | clearUserScoped() | clearUserScoped() |
| preload 需求 | 无 | 需要（cacheApi IPC） |
| IPC 安全 | N/A | cache:set 使用 JSON.stringify 传递 |
