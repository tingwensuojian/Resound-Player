# 本地歌曲功能 — 开发实现总结

> 最后更新: 2026-05-24 (v3.4 — 全面空值守卫 + 封面加载修复 + loadTracks 实现)

## 目录

1. [架构总览](#1-架构总览)
2. [v3 变更记录（2026-05-20）](#-v3-变更记录2026-05-20)
3. [v3.1 变更记录（2026-05-21）](#-v31-变更记录2026-05-21)
4. [v3.3 变更记录（2026-05-23）](#-v33-变更记录2026-05-23)
5. [v3.4 变更记录（2026-05-24）](#-v34-变更记录2026-05-24)
6. [文件清单](#2-文件清单)
7. [存储方案](#3-存储方案)
7. [IPC 三层链路](#4-ipc-三层链路)
8. [平台检测与守卫](#5-平台检测与守卫)
9. [渲染进程组件](#6-渲染进程组件)
10. [虚拟列表实现](#7-虚拟列表实现)
11. [扫描进度报告](#8-扫描进度报告)
12. [常见 Bug 与修复](#9-常见-bug-与修复)
13. [新增功能的操作步骤](#10-新增功能的操作步骤)

---

## 📦 v3 变更记录（2026-05-20）

> 目标：本地音乐页面视觉对齐歌单详情页模版。

| 变更 | 文件 | 说明 |
|------|------|------|
| **Hub 标签栏** | `LocalMusicHub.vue` | 自建 `.hub-tabs-wrap` → `DetailTabBar` 组件，pill 形状 + gradient active 态 |
| **歌曲列表视觉** | `VirtualSongList.vue` | 7 列 38px 表格 → 3 列 68px 富列表，52px 封面（radius 10px）、meta 上下布局 |
| **序号/播放/暂停** | `VirtualSongList.vue` | 自制 PlayPause 按钮：序号 → 播放 SVG → 暂停 SVG → 3 条弹跳音浪，点击当前歌曲触发 togglePlay |
| **表头同步** | `VirtualSongList.vue` | 简化为 3 列 grid，title+artist 合并为 inline flex |
| **本地歌单详情页** | `LocalPlaylistDetailPage.vue` | 全重构：自建固定标题+裸v-for → `DetailStickyHeroHeader` + `HeroCoverMedia` + `VirtualSongList` + `useDetailStickyState` + blur 背景 + 右键菜单移除
| **删除冗余** | `VirtualSongList.vue` | 移除未使用的 `formatDuration`、`localMusicStore` import、`.local-song-play`/`.local-song-idx` CSS |

---

## 📦 v3.1 变更记录（2026-05-21）

> 目标：修复封面存储格式 + 封面正方形显示。

| 变更 | 文件 | 说明 |
|------|------|------|
| **马赛克封面存 data URL** | `src/stores/localMusic.ts` | `generatePlaylistMosaic` 中 `pl.customCoverUrl = dataUrl`（取代 `res.filePath`），base64 data URL 可直接用于 `<img>` |
| **IPC 存 data URL 到 DB** | `electron/services/ipc/localMusicIpc.js` | `local:playlist-save-mosaic` handler 写入 `customCoverUrl: coverDataUrl`（取代 `customCoverUrl: filePath`） |
| **旧路径自动迁移** | `electron/services/ipc/localMusicIpc.js` | 新增 `resolveCustomCoverUrl` 辅助函数，在 `local:playlist-list`/`local:playlist-get` 中自动将文件路径转为 data URL 并更新 DB |
| **封面正方形显示** | `src/components/DetailStickyHeroHeader.vue` | `:deep(img.cover)` 加入 `aspect-ratio: 1; object-fit: cover`，无论图片原始宽高比始终显示为正方形 |

## 📦 v3.4 变更记录（2026-05-24）

> 目标：全面修复空值守卫 + 封面加载 + loadTracks / scanAll 实现。

| 变更 | 文件 | 说明 |
|------|------|------|
| **addDir 选目录** | `LocalStatsPage.vue` | `addDir()`/`handleScan()` 改为先调 `selectDirectory()`，防止 undefined 混入 directories 数组 |
| **handleScan 选目录** | `LocalSongsPage.vue` | 同上修复 |
| **addDirectory 空值守卫** | `localMusic.ts` | 入口加 `if (!dir) return`，拒绝 undefined |
| **loadDirectories 清洗** | `localMusic.ts` | 从 localStorage 解析后过滤 null/undefined |
| **saveDirectories 自愈** | `localMusic.ts` | 写入前检查并过滤脏数据 |
| **folderTree 空值守卫** | `localMusic.ts` | 遍历 directories 时跳过 null |
| **getDirLabel 空值守卫** | `LocalSongsPage.vue`, `LocalStatsPage.vue` | 加 `if (!dirPath) return ''` |
| **DB getTrackByPath 空值守卫** | `LocalMusicDB.js` | 加 `if (!filePath) return null` |
| **DB removeTracks 空值守卫** | `LocalMusicDB.js` | 过滤 null path，防止 `p.replace` 崩溃 |
| **Scanner scanDir 空值守卫** | `NodeMusicScanner.js` | 加 `if (!dirPath)` 直接返回 |
| **IPC local:scan 空值守卫** | `localMusicIpc.js` | 加 `if (!dirPath) return` |
| **loadTracks 实现** | `localMusic.ts` | 从空函数改为调用 `getAll()` 加载 DB 数据 + `lazyLoadCovers()` |
| **scanAll 重写** | `localMusic.ts` | 移除不存在的 `startScan`，遍历 directories 调 `scan()` + 注册 `onScanProgress` |
| **封面 API 名修复** | `localMusic.ts` | `readCover` → `getCover`（preload 实际暴露的 API 名） |
| **封面触发修复** | `localMusic.ts` | `loadTracks()` 末尾自动调 `lazyLoadCovers()`，所有路径都能加载封面 |

## 1. 架构总览

```
┌──────────────────────────────────────────────────────────────┐
│                    渲染进程 (Renderer)                         │
│                                                               │
│  src/stores/localMusic.ts      ← 响应式 Store                  │
│  src/components/LocalMusicHub.vue  ← 统一分栏 Hub 入口          │
│  src/views/Local*.vue          ← 6 个子视图页面                 │
│  src/components/Sidebar.vue    ← 侧栏菜单（1 个本地入口）        │
│  src/components/VirtualSongList.vue ← 自定义虚拟滚动列表        │
│  src/components/LocalContextMenu.vue ← 右键菜单               │
│  src/App.vue                   ← 路由注册 + 平台守卫           │
│  src/utils/platform.ts         ← LocalApi 类型 + 平台检测      │
│                                                               │
│  preload.js (contextBridge)    ← 暴露 localApi 对象到渲染进程   │
└──────────────────┬───────────────────────────────────────────┘
                   │ IPC (invoke/handle)
┌──────────────────▼───────────────────────────────────────────┐
│                    主进程 (Main Process)                      │
│                                                               │
│  electron/main.js               ← 入口 + 模块级 IPC           │
│  electron/services/             ← 运行时 JS 文件               │
│    ├── db/LocalMusicDB.js       ← SQLite 文件存储 (sql.js)     │
│    ├── scanner/NodeMusicScanner.js ← 音乐文件扫描器             │
│    ├── ipc/localMusicIpc.js     ← IPC 处理器注册              │
│    ├── CoverCache.js            ← 封面磁盘缓存                 │
│    └── types.ts                 ← TrackRecord 等类型定义        │
│                                                               │
│  注: 运行时使用直写 .js 文件，无需 TypeScript 编译             │
└──────────────────────────────────────────────────────────────┘
```

### 核心原则

- **仅桌面端（Electron）**：Web 端不提供本地歌曲功能
- **platform.isDesktop 守卫**：所有本地入口、路由、Store 方法都需加守卫
- **播放方案**：通过 IPC (`local:read-file`) 读取文件内容，在渲染进程创建 `Blob` + `URL.createObjectURL`，避免 `local://` 协议的 CORS 问题

---

## 2. 文件清单

### 主进程（Electron Main Process）

| 文件 | 类型 | 说明 |
|---|---|---|
| `electron/main.js` | JS | 入口文件，模块级 IPC，bootstrap 初始化 |
| `electron/preload.js` | JS | contextBridge 暴露 `localApi` 对象到 `window` |
| `electron/services/db/LocalMusicDB.js` | JS | **运行时文件** — SQLite 文件存储 (sql.js WASM)，ACID 事务 + 原子写入 |
| `electron/services/db/LocalMusicDB.ts` | TS | ❌ 已删除 — 消除双文件不一致问题 |
| `electron/services/scanner/NodeMusicScanner.js` | JS | **运行时文件** — 并发扫描器（8 worker），支持 12 种音频格式，mtime 增量扫描，外部 LRC 检测，文件收集阶段实时报告进度 |
| `electron/services/scanner/NodeMusicScanner.ts` | TS | TypeScript 源码（仅供类型参考） |
| `electron/services/ipc/localMusicIpc.js` | JS | 运行时文件 — 16+ 个 IPC handler（扫描/搜索/歌单CRUD/统计） |
| `electron/services/CoverCache.js` | JS | 封面磁盘缓存，`md5(path\|mtime)` 键 |

### 渲染进程（Renderer）

| 文件 | 说明 |
|---|---|
| `src/utils/platform.ts` | `LocalApi` 类型 + `platform.isDesktop` / `hasLocalMusicSupport` |
| `src/stores/localMusic.ts` | 响应式 Store，443 行，全守卫模式，含 `loadTracks`/`scanAll`/`lazyLoadCovers` 实现 |
| `src/stores/player.ts` | `formatTrack` 中新增 `'local'` 类型支持和 `path` 字段 |
| `src/views/LocalStatsPage.vue` | 扫描目录管理 + 最近添加 |
| `src/views/LocalSongsPage.vue` | 歌曲列表（搜索/排序/目录过滤/右键菜单），排序和目录过滤使用 `DropdownSelect` 组件 |
| `src/views/LocalArtistsPage.vue` | 歌手歌曲列表（全宽，配合 Hub 左栏索引过滤） |
| `src/views/LocalAlbumsPage.vue` | 专辑歌曲列表（全宽，配合 Hub 左栏索引过滤） |
| `src/views/LocalFoldersPage.vue` | 目录歌曲列表（全宽，配合 Hub 左栏目录树过滤） |
| `src/views/LocalPlaylistsPage.vue` | 歌单卡片网格（新建/删除） |
| `src/views/LocalPlaylistDetailPage.vue` | 本地歌单详情（吸顶头部 + 封面 + VirtualSongList + 右键移除 + PromptModal 重命名/添加曲目），使用 `PromptModal` 替代原生 prompt |
| `src/components/LocalMusicHub.vue` | 统一分栏 Hub，标题 + 公共统计栏 + 标签栏（DetailTabBar 组件）+ 条件左栏索引 + 右栏内容 |
| `src/views/FolderTreeNode.vue` | 递归文件夹树节点组件（lucide-vue-next 图标、展开/折叠动画、缩进引导线） |
| `src/components/LocalContextMenu.vue` | 通用右键菜单组件 (Teleport) |
| `src/components/ui/PromptModal.vue` | 输入弹窗组件，替代 Electron 不支持的 `window.prompt()` |
| `src/components/ui/DropdownSelect.vue` | 通用下拉选择组件（Teleport to body，自动定位） |
| `src/components/Sidebar.vue` | 侧栏菜单（1 个本地入口） |
| `src/App.vue` | 路由注册 + platform.isDesktop 守卫 + `handleLocalNavigate` 本地页面路由 |

## 3. 存储方案

采用 **SQLite 文件存储**（`local-music.sqlite` 位于 Electron `userData` 目录），通过 `sql.js` (WASM SQLite) 实现 ACID 事务和索引查询。

### 演进历史

| 方案 | 版本 | 问题 | 结论 |
|-----|------|------|------|
| `better-sqlite3` | 初始 | Electron 42 的 V8 API 变更导致 `electron-rebuild` 编译失败 | ❌ 放弃 |
| **JSON 文件** | 中间过渡 | 写入不安全（无原子性、崩溃后半写文件丢数据） | ❌ 放弃 |
| `sql.js` (WASM SQLite) | **当前** | 零原生依赖，纯 WASM 在 Electron 主进程稳定工作 | ✅ 采用 |

### 数据库 Schema (4 表 + 5 索引)

```sql
tracks:          id(PK), path(UNIQUE), title, artist, album, albumArtist,
                 duration, bitrate, sampleRate, trackNo, discNo, genre,
                 year, coverPath, fileSize, mtime, hasLyrics, createdAt, updatedAt
                 → idx_tracks_path, idx_tracks_title, idx_tracks_artist, idx_tracks_album

scan_dirs:       id(PK AUTOINCREMENT), path(UNIQUE), label, lastScan

playlists:       id(PK), name, description, coverPath, createdAt, updatedAt

playlist_tracks: (playlistId, trackId)(PK), sortOrder, addedAt
                 → FOREIGN KEY → playlists(id) ON DELETE CASCADE, tracks(id) ON DELETE CASCADE
                 → idx_playlist_tracks_playlist
```

### 读写机制

- **写入**：通过 `#enqueueWrite(fn)` 串行队列，所有写操作在 Promise 链中排队执行
- **原子持久化**：`#atomicPersist()` → `db.export()` 导出完整 SQLite 二进制 → `writeFileSync(.tmp)` → `renameSync(.tmp → .sqlite)` — POSIX 原子替换
- **崩溃恢复**：WAL journal 模式 + try/catch 保护，JSON 方案时的半写文件问题已解决
- **查询**：SQL 索引查询（`COLLATE NOCASE` 排序），取代 JSON 方案的 JS `filter`
- **数据迁移**：首次启动时自动从旧 `local-music.json` 迁移到 SQLite

## 4. IPC 三层链路

所有本地音乐操作都经过 **DB → IPC → Preload → Platform Type → Store → View** 六层：

```
LocalMusicDB.js           ← SQLite 查询（索引 + COLLATE NOCASE）
    ↓
localMusicIpc.js          ← ipcMain.handle('local:xxx', ...)
    ↓
preload.js               ← contextBridge.exposeInMainWorld('localApi', {...})
    ↓
src/utils/platform.ts    ← LocalApi 接口类型定义
    ↓
src/stores/localMusic.ts ← 响应式 Store，调用 platform.localApi.xxx()
    ↓
src/views/Local*.vue     ← UI 组件读写 Store
```

### 21 个 IPC 通道一览

| 通道 | 用途 | 依赖 |
|---|---|---|
| `select-directory` | 选择扫描目录 | 无（模块级注册） |
| `local:scan` | 扫描目录 | Scanner + DB |
| `local:search` | 搜索曲库 | DB (SQL LIKE 查询 + COLLATE NOCASE 排序) |
| `local:get-all` | 获取全部歌曲 | DB |
| `local:track-count` | 获取歌曲总数 | DB |
| `local:get-lyric` | 读取歌词文件 | fs |
| `local:get-cover` | 获取封面(带缓存) | CoverCache |
| `local:read-file` | 读取文件 | fs |
| `local:playlist-create` | 创建歌单 | DB |
| `local:playlist-list` | 歌单列表 | DB |
| `local:playlist-get` | 歌单详情 | DB |
| `local:playlist-delete` | 删除歌单 | DB |
| `local:playlist-rename` | 重命名歌单 | DB |
| `local:playlist-add-track` | 歌单添加曲目 | DB |
| `local:playlist-remove-track` | 歌单移除曲目 | DB |
| `local:playlist-tracks` | 歌单曲目列表 | DB |
| `local:get-recent` | 最近添加 | DB |
| `local:get-stats` | 统计信息 | DB |
| `local:clear-all` | 清除所有歌曲数据 | DB |
| `local:remove-tracks-by-dir` | 按目录删除歌曲 | DB |
| `local:remove-tracks` | 按路径批量删除歌曲 | DB |

### 添加新 IPC 的步骤

1. **DB 层** (`LocalMusicDB.js`) — 添加查询方法
2. **IPC 层** (`localMusicIpc.js`) — 注册 `ipcMain.handle`
3. **Preload** (`preload.js`) — `contextBridge` 暴露 API
4. **类型** (`platform.ts`) — `LocalApi` 接口添加签名
5. **Store** (`localMusic.ts`) — 添加调用方法
6. **重启 Electron**

## 5. 平台检测与守卫

### `platform.isDesktop` 检测逻辑

```typescript
// src/utils/platform.ts
get isDesktop(): boolean {
    if (typeof window === 'undefined') return false
    // Electron preload 注入 appEnv
    if (!!(window as any).appEnv?.isDesktop) return true
    // 降级：UA 检测
    return typeof navigator !== 'undefined' && navigator.userAgent.includes('Electron')
}
```

### 前置条件：preload 脚本

`electron/preload.js` 必须使用 **CommonJS** (`require`) 而非 ESM (`import`)：

```js
// ✅ Electron 沙盒环境兼容
const { contextBridge, ipcRenderer } = require('electron');

// ❌ 不支持（Sandbox 模式下 SyntaxError）
import { contextBridge, ipcRenderer } from 'electron';
```

### 守卫应用位置

| 位置 | 守卫方式 |
|---|---|
| `App.vue` 路由 | `v-if="activePage === 'local-xxx' && platform.isDesktop"` |
| `Sidebar.vue` 菜单 | `...(platform.isDesktop ? [{...}] : [])` |
| Store 方法 | `if (!platform.localApi) return` |
| SettingsPage 设置项 | `!platform.isDesktop` 时从 `currentGroups` 过滤 |
| `apiBaseUrl` | Dev 模式返回 `/api`（走 Vite 代理），打包后返回直连 URL |

## 6. 渲染进程组件

### Store: `localMusic.ts`

| 状态 | 类型 | 说明 |
|---|---|---|
| `tracks` | `LocalTrack[]` | 全部歌曲 |
| `directories` | `string[]` | 扫描目录列表 |
| `scanning` | `boolean` | 是否正在扫描 |
| `progress` | `{current, total}` | 扫描进度 |
| `searchKeyword` | `string` | 搜索关键词 |
| `sortField/Order` | `SortField/SortOrder` | 排序状态 |
| `activeView` | `LocalView` | Hub 当前激活的标签页（stats/songs/artists/albums/folders/playlists） |
| `selectedArtist` | `string` | 左栏选中的歌手名 |
| `selectedAlbum` | `string` | 左栏选中的专辑名 |
| `selectedFolderPath` | `string` | 左栏选中的目录路径 |
| `collapsedFolders` | `Set<string>` | 目录树中已折叠的节点路径集合 |
| `playlists` | `any[]` | 歌单列表 |
| `activePlaylistDetail` | `any \| null` | 当前打开的歌单 |

### 视图页面

本地音乐页面统一由 `LocalMusicHub.vue` 作为入口。页面结构从上到下依次为：
1. **页面标题** — "本地音乐"
2. **公共统计栏 + 标签栏** — 统计卡片（歌曲/歌手/专辑/总时长/总大小）与 6 个标签按钮左右并排
3. **内容区** — 左栏（歌手/专辑/目录索引，条件渲染）+ 右栏（子视图内容）

子页面不自带分栏结构，统一由 Hub 管理。

| Hub 标签 | 子组件 | 左栏 | 右栏内容 |
|---|---|---|---|
| 概览 | `LocalStatsPage` | 无 | 扫描目录管理 + 最近添加 |
| 歌曲 | `LocalSongsPage` | 无 | 搜索/排序/过滤 + 歌曲网格列表 |
| 歌手 | `LocalArtistsPage` | 歌手索引列表（点击过滤） | 歌手歌曲列表（`selectedArtistTracks`） |
| 专辑 | `LocalAlbumsPage` | 专辑索引列表（点击过滤） | 专辑歌曲列表（`selectedAlbumTracks`） |
| 目录 | `LocalFoldersPage` | 可折叠目录树（点击过滤） | 目录歌曲列表（`selectedFolderTracks`） |
| 歌单 | `LocalPlaylistsPage` | 无 | 歌单卡片网格（新建/删除） |

| 独立路由 | 组件 | 布局 |
|---|---|---|
| `local-playlist-detail` | `LocalPlaylistDetailPage` | 单列：歌曲列表 + 操作按钮 |

### 跨页面导航

本地页面统一由 `LocalMusicHub.vue` 承载，通过 `activeView` 在 6 个标签间切换。子页面间导航使用 `CustomEvent('local-navigate', { detail: { page: 'local-xxx' } })`，App.vue 监听并处理：`activePage.value = detail.page`（用于 "定位到目录" 等场景）。

## 7. 常见 Bug 与修复

### 运行时错误

| 错误 | 原因 | 修复 |
|---|---|---|
| `ERR_MODULE_NOT_FOUND: Cannot find module ...LocalMusicDB.js` | TypeScript 文件未编译为 JS | 用 esbuild 编译 `.ts` → `.js` |
| `Cannot use import statement outside a module` (preload 脚本) | preload 沙盒不支持 ESM | 改用 CommonJS `require()` |
| `No handler registered for 'select-directory'` | IPC handler 未在模块顶层注册 | 移到 `localMusicIpc.ts` 模块级 |
| `Attempted to register a second handler for 'select-directory'` | handler 被注册了两次 | 只保留一处注册 |
| `TypeError: SQLite3 can only bind numbers, strings, bigints, buffers, and null` | 布尔值传入 SQLite | `Boolean()` → `(... ? 1 : 0)` |
| `TypeError: fs.existsSync is not a function` | `fs/promises` 无 `existsSync` | 改用 `import { existsSync } from 'fs'` |
| CORS: `Access-Control-Allow-Origin: *` with credentials | Dev 模式直连 API 端口 | `apiBaseUrl` 返回 `/api` 走 Vite 代理 |
| `[Vue warn]: Failed to resolve component: LocalMusicHub` | `App.vue` 中使用了 `<LocalMusicHub>` 但未 import | 添加 `import LocalMusicHub from './components/LocalMusicHub.vue'` |

### 空值守卫

| 错误 | 原因 | 修复 |
|---|---|---|
| `Cannot read properties of null (reading 'replace')` — `getDirLabel` | `directories` 数组混入 undefined，`dirPath.replace` 崩溃 | ① store `loadDirectories` 解析后过滤 null ② `addDirectory` 加 `if (!dir) return` ③ `getDirLabel` 加空值守卫 |
| `Cannot read properties of undefined (reading 'replace')` — `removeTracksByDirectory` | undefined dirPath 穿透到 DB 层 | ① DB `removeTracksByDirectory` 加 `if (!dirPath)` ② 页面 `removeDir` 加 `if (!dir) return` |
| `removeTracks` 对 null path 调用 `p.replace` 崩溃 | `paths` 数组中混入 null | DB 层加 `paths.filter(p => p)` |
| `getTrackByPath` 对 null filePath 调用 `filePath.replace` 崩溃 | 调用方可能传 null | DB 层加 `if (!filePath) return null` |
| `scanDir` 收到 null dirPath | 用户取消目录选择 | Scanner 加 `if (!dirPath)` 守卫 |
| `addDir()` 推入 undefined 到 directories 数组 | `localMusicStore.addDirectory()` 调用时不传参数 | `addDir()` 先调 `selectDirectory()` 获取目录路径再传参 |

### Store / Type 错误

| 错误 | 原因 | 修复 |
|---|---|---|
| 本地歌曲被识别为 `source: 'song'` 而非 `'local'` | `formatTrack` 中未处理 `local` | 添加 `raw.source === 'local' ? 'local'` 分支 |
| 播放时 `id: NaN` | md5 字符串 ID 被当数字处理 | 保留 `source: 'local'` 即可走 local:// 协议 |
| `hasLocalSupport` 返回 `false` | `isDesktop` 为真但 `window.localApi` 不存在 | preload 脚本加载失败 → 改用 `require` |
| `Cannot set properties of undefined (setting 'selectedFolderPath')` | 将 reactive 方法直接作为回调传递导致 `this` 丢失 | 模板中使用箭头函数包裹：`@select="(p) => store.method(p)"` |
| `directories` 数组始终为空 | `loadDirectories()` 从未被调用 | 在需要的组件 `onMounted` 中调用 `localMusicStore.loadDirectories()` |

### 写入队列时序

| Bug | 原因 | 修复 |
|---|---|---|
| `clearAll()` 后刷新数据"复活" | `_dataCleared` 护城河掩盖了 DB 删除失败；`clearAllTracks` 跳过 `serializedWrite` 导致写冲突 | 移除 `_dataCleared`；`clearAll()` 末尾调用 `loadTracks()` 确保 store 与 DB 同步；`clearAllTracks` 使用 `serializedWrite` |
| `removeTracks` 删除未生效 | `serializedWrite` 队列导致 `loadTracks()` 在 SQL 执行前读取旧数据 | 写操作改直接同步事务（`this.db.transaction`），删除立即生效后再 `loadTracks()` |

### CSS 布局

| Bug | 原因 | 修复 |
|---|---|---|
| 排序菜单被父容器 overflow 裁切 | `.sort-menu` 使用 `position: absolute + left: 172px` 硬编码定位，上层 `.hub-content` 有 `overflow: auto` | 替换为 `DropdownSelect`（Teleport to body + `position: fixed`） |
| 目录过滤原生 `<select>` 样式不统一 | 使用原生 `<select>`，与项目设计系统脱节 | 替换为 `DropdownSelect` 组件 |
| 搜索框 focus 环被裁切 | 默认 `outline` 在 focus 时延伸到元素外，被父容器裁切 | 改为 `inset box-shadow` focus 环，保持在元素内部 |
| 歌曲行与详情页风格统一 | VirtualSongList 7 列 38px 表格布局与 PlaylistDetailPage 的 68px 富列表差异大 | 改为 3 列 68px grid（idx/52px cover/meta），合并 idx 和 play 按钮为自制 PlayPause（序号→播放→暂停→音浪），表头同步 |

### Store 持久化

| Bug | 原因 | 修复 |
|---|---|---|
| 移除目录后自动恢复 | `saveDirectories()` 先读 localStorage 旧数据合并到 `this.directories`，把刚删的目录又加回来 | `removeDir()` 和 `clearAll()` 中先手动从 localStorage 移除/清空该目录，再调 `saveDirectories()` |

### 封面加载

| Bug | 原因 | 修复 |
|---|---|---|
| 首次扫描后封面不显示，需刷新才出现 | `lazyLoadCovers()` 使用 `setTimeout(loadNext, 30)` 逐首加载，100 首歌需 3 秒；且首次渲染时 `coverUrl` 全为空 | ① `loadTracks()` 中 `coverUrl` 从 `_coverCache` 预填充（缓存命中即显示）② `lazyLoadCovers()` 改为 `Promise.all` 全量并行，无分批延迟 |
| 歌单马赛克封面显示为破碎图片 | `generatePlaylistMosaic` 将 `customCoverUrl` 存为本地文件系统路径（如 `/Users/.../mosaic-covers/playlist-xxx.jpg`），Electron 渲染进程的 `<img>` 无法加载本地路径 | ① `generatePlaylistMosaic` 存 `dataUrl`（base64 data URL）而非 `res.filePath` ② IPC handler `local:playlist-save-mosaic` 将 `coverDataUrl` 写入 DB ③ 新增 `resolveCustomCoverUrl` 在 `local:playlist-list`/`local:playlist-get` 中自动迁移旧文件路径 |
| 本地歌单详情页封面显示为长方形而非正方形 | `:deep(img.cover)` 只有 `width: 100%; height: 100%`，无 `aspect-ratio` 约束；本地封面不一定是正方形（音频嵌入封面可能为长方形），按自然尺寸渲染 | 在 `DetailStickyHeroHeader.vue` 的 `:deep(img.cover)` 中加入 `aspect-ratio: 1; object-fit: cover` |
| 概况页"最近添加"有封面，歌曲列表页无封面 | `loadTracks()` 把 `coverUrl` 置空，但 `lazyLoadCovers()` 只在 `scanAll()` 中触发；DB 已有数据时页面加载不走扫描路径，封面永不加载 | `loadTracks()` 末尾自动调用 `lazyLoadCovers()`，不论从哪个入口加载数据都触发封面加载 |
| `lazyLoadCovers()` 调用 `readCover` 永远失败 | API 名称不匹配：preload 暴露的是 `getCover`，但代码写的是 `readCover`，调用失败被 catch 静默吞掉 | `readCover` → `getCover` |

### 最近添加不刷新

| Bug | 原因 | 修复 |
|---|---|---|
| 扫描完成后「最近添加」列表不更新，需切标签页 | `recent` 列表仅在 `onMounted` 加载一次，扫描后未自动刷新 | 在 `LocalStatsPage.vue` 添加 `watch(() => localMusicStore.tracks.length)` 自动重新加载 |

### loadTracks / scanAll 未实现

| Bug | 原因 | 修复 |
|---|---|---|
| 删除目录后列表不刷新 | `loadTracks()` 是空函数，删除流程中调用它但什么都不做 | 实现 `loadTracks()` — 调用 `platform.localApi.getAll()` 从 DB 加载全部歌曲到 store |
| `scanAll` 调用不存在的 `startScan` | `platform.localApi.startScan` 从未在 preload 中定义，调用无效果 | `scanAll` 改为遍历 `state.directories` 逐个调 `platform.localApi.scan(dir)` |
| 扫描进度不更新 | 未注册 `onScanProgress` 监听器，扫描事件无处接收 | `scanAll` 中注册 `onScanProgress`，处理 progress/error 事件，扫描完成清理监听 |

### Electron 兼容性

| Bug | 原因 | 修复 |
|---|---|---|
| `npm run dev:desktop` 没有弹出桌面窗口 | Electron 42.1.0 的 pnpm store 缓存中二进制文件损坏（extract-zip symlink 冲突） | 清除 pnpm store 中 `electron@42.1.0` 缓存后重新 `pnpm install` |
| `No handler registered for 'local:*'` IPC 全部报错 | `better-sqlite3` 原生模块不兼容 Electron 42 的 V8 API，编译失败导致 `registerLocalMusicIpc` 未执行 | 改用 JSON 文件存储，移除 `better-sqlite3` 和 `sql.js` 依赖 |
| `SyntaxError: Unexpected strict mode reserved word` | `.js` 文件中混入了 TypeScript 语法（`interface`、`private`、类型标注） | 重写 `.js` 文件为纯 JavaScript |
| `Failed to construct 'URL': Invalid URL`（Vite 开发模式） | Vite 模块热替换系统在解析动态 `import()` 路径时的内部错误，仅开发模式出现 | 不影响功能，已添加全局 `unhandledrejection` 日志捕获 |
| `prompt() is not supported`（Electron 渲染进程） | Electron 渲染进程不支持原生 `window.prompt()` | 创建 `PromptModal.vue` 组件替代，使用 Teleport 弹窗 + v-model |

### @tanstack/vue-virtual v3 兼容性

| Bug | 原因 | 修复 |
|---|---|---|
| `getVirtualItems is not a function` | v3 的 `useVirtualizer` 返回 `ShallowRef<Virtualizer>` 而非直接返回实例 | 加 `.value` 访问 `virtualizer.value.getVirtualItems()` |
| `getTotalSize()` 始终返回 0 | v3 的 `getMeasurements()` memo 缓存永不失效，`getSize()` 依赖 `scrollRect` 形成死锁 | 移除 `@tanstack/vue-virtual` 依赖，改用自定义虚拟滚动实现 |
| 自定义虚拟滚动无法显示内容 | v3 的 `getMeasurements()` 内部 `initialMeasurementsCache` 未设置时返回空数组 | 自行实现定高虚拟滚动（初始 38px/行 → v3 改为 68px/行 + 52px 封面 + 播放暂停音浪），`ResizeObserver` 测量容器高度 + `scrollTop` 计算可见范围 |

### 本地播放 CORS

| Bug | 原因 | 修复 |
|---|---|---|
| 本地歌曲播放时报 CORS: `local://` 被拦截 | 开发模式页面从 `http://localhost:5173` 加载，`local://` 协议跨源被浏览器拦截 | 通过 IPC (`local:read-file`) 读取文件 ArrayBuffer，创建 `Blob` + `URL.createObjectURL` |

### 扫描进度

| Bug | 原因 | 修复 |
|---|---|---|
| 扫描大目录（27000+ 文件）时 UI 卡在"正在扫描 0/0" | `collectFiles` 遍历目录树期间没有 yield 任何进度事件，持续数十秒 | 将 `collectFiles` 放到后台运行，轮询 `state.found` 计数器，每 300ms yield 一次进度 |

### 本地导航

| Bug | 原因 | 修复 |
|---|---|---|
| `← 返回歌单` 跳转到空白页 | `goBack()` 只改了 store 的 `activeView`，未 dispatch `local-navigate` 事件通知 App.vue 切换页面路由；后续 dispatch 的 `'local-playlists'` 不在 App.vue 渲染路由中，且 whitelist 缺少 `'local-music'` | ① `goBack()` 中 dispatch `local-navigate` 事件 ② `handleLocalNavigate` whitelist 增加 `'local-music'` |

## 9. 新增功能

### 清除所有数据

在概览页（`LocalStatsPage.vue`）新增「清除所有数据」按钮：

- 点击后确认对话框 -> 调用 `localMusicStore.clearAll()`
- 清空扫描目录列表、清空 DB 中所有歌曲记录、清空 store、清空封面缓存
- 通过 `_statsRefresh` 计数器触发概况栏刷新

涉及变更：

| 层次 | 文件 |
|---|---|
| DB | `LocalMusicDB.ts/js` — 新增 `clearAllTracks()` 方法（串行化事务：先删 `playlist_tracks` → `tracks` → `scan_dirs`） |
| IPC | `localMusicIpc.ts/js` — 新增 `local:clear-all` handler，带 try/catch 错误处理 |
| Preload | `preload.js` — 暴露 `clearAllData` |
| 类型 | `platform.ts` — `LocalApi` 添加 `clearAllData` 签名 |
| Store | `localMusic.ts` — 新增 `clearAll()` 方法 |
| UI | `LocalStatsPage.vue` — 新增清除按钮 + `clearAllData` 处理函数 |

### 歌曲页多选删除

在歌曲列表页（`LocalSongsPage.vue`）新增多选模式：

- 操作栏新增「选择」按钮，点击进入多选模式
- 表头和每行新增 checkbox 列
- 选中模式操作栏：全选/取消 + 已选计数 + 删除选中按钮
- 删除后调用 `loadTracks()` 刷新列表

涉及变更：

| 层次 | 文件 |
|---|---|
| IPC | `localMusicIpc.ts/js` — 新增 `local:remove-tracks` handler |
| Preload | `preload.js` — 暴露 `removeTracks(paths)` |
| 类型 | `platform.ts` — `LocalApi` 添加 `removeTracks` 签名 |
| UI | `LocalSongsPage.vue` — 添加多选和删除功能（~100 行新增） |
| DB | `LocalMusicDB.ts/js` — `removeTracks` 改为直接同步事务，确保删除后 `loadTracks()` 读到最新状态 |

### 移除目录时同步删除数据库

在概览页移除扫描目录时，同步删除该目录下的歌曲：

- `removeDir` 函数先调用 `deleteTracksByDirectory`（按目录前缀删除 DB 记录）
- 再从 store 移除目录，调用 `loadTracks()` 重新加载
- 新增 `local:remove-tracks-by-dir` IPC handler

### 右键定位到目录

在歌曲列表页右键菜单新增「定位到目录」，点击后跳转到本地音乐的「目录」标签页，自动展开目录树、高亮目标文件夹并滚动到该首歌曲。

**数据流：**

```
LocalSongsPage.vue → showInFolder()
  ① getTreePath(track.path)  →  将文件绝对路径转为树路径（相对扫描目录）
  ② expandFolderAncestors()  →  从 collapsedFolders 移除所有祖先节点
  ③ selectedFolderPath = treePath  →  目录树选中该节点
  ④ locatedTrackId = track.id  →  记录待高亮歌曲
  ⑤ activeView = 'folders'  →  切换到「目录」标签
  ⑥ dispatch local-navigate →  App.vue 切到本地音乐页面
```

**涉及变更：**

| 层次 | 文件 | 说明 |
|---|---|---|
| Store | `localMusic.ts` | 新增 `getTreePath()`（绝对路径→树路径转换）、`expandFolderAncestors()`（展开祖先节点）、`locatedTrackId` 字段 |
| 右键菜单 | `LocalSongsPage.vue` | `showInFolder` 改用 `getTreePath` + 设置 `locatedTrackId` + 设置 `activeView` + 导航到 `local-music` |
| 文件夹页 | `LocalFoldersPage.vue` | 传递 `:highlighted-id="localMusicStore.locatedTrackId"` 给列表组件；播放时清除高亮 |
| 歌曲列表 | `VirtualSongList.vue` | 新增 `highlightedId` prop（区别于 `nowPlayingId`）；`.highlighted` class（半透明 accent 背景 + 描边轮廓）；`scrollToHighlighted()` 自动滚动到目标行 |
| 滚动探测 | `VirtualSongList.vue` | `findScrollParent()` 向上遍历 DOM 找到实际滚动容器（防止 `.vl-container`/`.hub-content` 二选一问题） |

### 歌曲行内操作栏（LocalSongActions）

参照 `SongActions.vue`（在线歌曲操作栏）模式，为本地歌曲列表添加悬停时显示的行内操作按钮。

**架构：**

```
LocalSongActions.vue  ← 新建，与 SongActions 同风格
  ├─ 播放（sa-btn）         ← 直显
  ├─ 下一首播放（sa-btn）    ← 直显
  ├─ 收藏至歌单（sa-btn）    ← 直显
  └─ 更多操作（sa-btn ⋮）    ← 下拉菜单（Teleport to body）
       ├─ 定位到目录
       ├─ 查看本地专辑
       ├─ 查看在线专辑
       └─ 上传至云盘
```

**关键机制：**

| 机制 | 说明 |
|---|---|
| hover 显隐 | 全局 CSS `.local-song-row:hover .song-actions`（与 SongActions 一致） |
| 定位 | `position: absolute; right: 8px; top: 50%; transform: translateY(-50%)` 悬浮在行右侧 |
| 按钮样式 | `sa-btn`：34×34px、10px 圆角、hover 上浮 -1px + accent 边框 |
| 更多菜单 | `Teleport to="body"` → 自适应上下方向（up/down）+ 左右防溢出 |
| 事件冒泡 | `LocalSongActions` emit → `VirtualSongList` 追加 track/index → 父页面处理 |

**涉及变更：**

| 文件 | 变更 |
|---|---|
| `src/components/ui/LocalSongActions.vue` | **新建** — 3 个直显 sa-btn + 更多菜单（4 项），自适应定位 |
| `src/components/VirtualSongList.vue` | 新增 `play-next`、`add-to-playlist`、`show-in-folder`、`show-local-album`、`show-online-album`、`upload-to-cloud` emits |
| `src/views/LocalSongsPage.vue` | 绑定事件 → `addToQueue(track, true)` / `addToPlaylist` / `showInFolder` / `showLocalAlbum` / `showOnlineAlbum` / `uploadToCloud` |
| `src/views/LocalFoldersPage.vue` | 同上；新增 `playNext`、`addToPlaylist` + 歌单选择对话框 |
| `src/views/LocalAlbumsPage.vue` | 传递 `:highlighted-id="localMusicStore.locatedTrackId"` |
| `src/views/LocalPlaylistDetailPage.vue` | 自建 v-for 行中添加 `<LocalSongActions>`；新增全部 6 个事件处理函数 + 歌单选择对话框；修复 CSS（`position: relative` + `overflow: visible`）；补全 PP 按钮 hover 交互（`@mouseenter`/`@mouseleave` + `isTrackPaused()` 暂停态图标逻辑） |
| `src/components/LocalContextMenu.vue` | 样式对齐 `SongActions` 更多菜单（`.more-menu`/`.more-menu__item`） |

### 查看本地专辑

点击「更多 → 查看本地专辑」，跳转到本地音乐的「专辑」标签页，左侧专辑列表自动选中该专辑、右侧歌曲列表高亮该歌曲。

**数据流：**

```
LocalSongActions → emit show-local-album
  → VirtualSongList → 追加 (track, index)
  → 页面 showLocalAlbum(track)
    ① selectedAlbum = track.album      → 左侧专辑列表选中
    ② locatedTrackId = track.id        → 高亮该行
    ③ activeView = 'albums'            → 切换到专辑标签
    ④ dispatch local-navigate          → 导航到本地音乐
```

### 查看在线专辑

点击「更多 → 查看在线专辑」，通过网易云 API 搜索匹配的在线专辑，跳转到在线 `AlbumDetailPage`。

**双阶段搜索策略（提高匹配精度）：**

```
阶段 1：按「歌曲名 + 歌手」搜 (type=1, limit=3)
  → 取 song.al.id（匹配到的歌曲自带专辑 ID，精度最高）
阶段 2（回退）：按「歌手 + 专辑名」搜 (type=10, limit=1)
  → 取 albums[0].id（歌曲搜索失败时兜底）
```

**导航链路：**

```
页面 showOnlineAlbum(track)
  → searchMusic(keywords, { type: 1, limit: 3 })
  → 取 song?.al?.id
  → dispatchEvent('open-album-detail', { albumId })
  → App.vue handleOpenAlbumDetail()
  → openAlbumDetail(albumId, 'local-music')
  → 切换到 AlbumDetailPage
```

**涉及变更：**

| 文件 | 变更 |
|---|---|
| `src/views/LocalSongsPage.vue` | `showOnlineAlbum(track)` — 双阶段搜索 + toast 提示 |
| `src/views/LocalFoldersPage.vue` | 同上 |
| `src/App.vue` | 新增 `handleOpenAlbumDetail` 监听 `open-album-detail` window 事件 |

### 上传至云盘

点击「更多 → 上传至云盘」，读取本地文件计算 MD5 后调用网易云 `/cloud/import` API 将歌曲导入云盘。

**全链路：**

```
用户点击 → 登录校验（未登录弹窗、非 cookie/qr 登录提示不支持）
  → platform.localApi.computeFileMd5(path)     ← Electron IPC
     → 主进程用 Node.js crypto 计算 MD5 + fs.stat 获取大小
  → 推断 fileType / bitrate
  → importToCloud({ song, fileType, fileSize, bitrate, md5, artist, album })
  → 成功/失败 toast
```

**涉及变更（6 层）：**

| 层 | 文件 | 变更 |
|---|---|---|
| IPC 主进程 | `electron/services/ipc/localMusicIpc.js` | 新增 `crypto` 导入 + `local:compute-file-md5` handler |
| preload | `electron/preload.js` | 暴露 `computeFileMd5(filePath)` |
| 类型 | `src/utils/platform.ts` | `LocalApi` 接口添加 `computeFileMd5` 签名 |
| 按钮 | `src/components/ui/LocalSongActions.vue` | 更多菜单中新增「上传至云盘」项 |
| 透传 | `src/components/VirtualSongList.vue` | 新增 `upload-to-cloud` emit |
| 处理 | `src/views/LocalSongsPage.vue` | `uploadToCloud(track)` — 完整导入流程 |
| 处理 | `src/views/LocalFoldersPage.vue` | 同上 |

### 通知系统接入

| 操作 | Toast | 类型 |
|---|---|---|
| 下一首播放 | "已加入播放队列" | success / 3s |
| 添加到歌单 | "已添加到歌单" | success / 3s |
| 查看在线专辑（成功） | "已跳转到在线专辑，若信息有误请使用搜索查找" | warning / 4s |
| 查看在线专辑（未找到） | "未找到在线专辑" | warning / 3s |
| 上传至云盘（成功） | "已上传至云盘" | success / 3s |
| 上传至云盘（失败） | "上传失败，请稍后重试" / "上传至云盘失败" | warning/error / 3s |

### 新增 IPC 通道

以新增 `local:xxx` 为例：

```
1. electron/services/db/LocalMusicDB.js
   → 添加查询方法 getXxx()

2. electron/services/ipc/localMusicIpc.js
   → ipcMain.handle('local:xxx', async () => db.getXxx())

3. electron/preload.js
   → xxx: () => ipcRenderer.invoke('local:xxx'),

4. src/utils/platform.ts
   → LocalApi 接口添加: xxx(): Promise<...>

5. src/stores/localMusic.ts
   → 添加 async method: this.platform.localApi.xxx()

6. 重启 Electron
   → main.js 改动需完全重启，Vite HMR 不影响主进程
```

### 新增页面

```
1. 创建 src/views/LocalMyNewPage.vue（遵循 .local-page shell 模式）
2. src/App.vue 中:
   - import LocalMyNewPage
   - 添加 v-else-if="activePage === 'local-my-new' && platform.isDesktop"
3. src/components/Sidebar.vue 中:
   - 添加 { key: 'local-my-new', label: '...', icon: ... }
4. 如果涉及 IPC, 执行上面的新增 IPC 步骤
5. lint + build 验证
```

### 新增 DB 字段/索引

```sql
-- SQLite: 使用 ALTER TABLE 添加字段
ALTER TABLE tracks ADD COLUMN newField TEXT NOT NULL DEFAULT '';
-- 或直接修改 SCHEMA 常量中的 CREATE TABLE 语句（对已有表无效，仅新创建的表生效）
```

> 迁移指南：对已有数据库的字段变更需编写迁移 SQL，通过 `#db.run()` 在应用启动时执行。

---

## 附录：依赖

| 包 | 用途 | 位置 |
|---|---|---|
| `music-metadata` | 音频元数据解析 | 主进程 |
| `jschardet` + `iconv-lite` | LRC 文件编码检测 (GBK→UTF-8) | 主进程 |
| `sql.js` | SQLite WASM 引擎（零原生依赖） | 主进程 |

> **注意**：当前存储方案为 SQLite（通过 `sql.js` WASM 引擎），纯 JavaScript 实现，无需原生模块编译。`electron-rebuild` 无需运行。仅当 Electron 版本升级后 `sql.js` 的 WASM 加载失败时需要检查兼容性。