# 端口自动探测与服务管理说明

## 背景

Mac 桌面端开发时涉及 **4 个本地服务**，之前使用固定端口，当端口被占用时会导致启动失败或冲突。

```
Vite Dev Server   → 5173
Netease API       → 38761
Unblock Proxy     → 38762
Unblock Match     → 38763
```

端口探测功能让这些服务在端口被占用时自动切换到可用端口，且各服务之间正确感知彼此的地址。

补充说明：当前 mac 打包版也会在应用启动时**内嵌拉起本地服务**。也就是说，`38761 / 38762 / 38763` 不只是开发环境端口，生产桌面包同样依赖这 3 个本地服务完成 API、解灰与匹配能力。

---

## 架构总览

```
┌─────────────────────────────────────────────────────────┐
│                  Electron 主进程                           │
│  main.js                                                 │
│    ├── port-manager.js      ← 端口探测                    │
│    ├── serviceManager.js    ← 启动/管理/终止后端服务       │
│    ├── unblock-native-match.js ← 内置 match 能力（首选）  │
│    └── preload.js           ← 暴露端口 + unblockBridge    │
└──────────────────────┬──────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────┐
│              开发模式启动脚本                              │
│  scripts/start-desktop.mjs                               │
│    ├── 先杀残留进程                                       │
│    ├── 探测 4 个端口                                      │
│    ├── 启动 vite + unblock-proxy + unblock-match(fallback)│
│    ├── 把端口注入 SERVICE_PORTS 环境变量                   │
│    └── 3 秒后启动 Electron                                │
└──────────────────────┬──────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────┐
│                Vite 开发服务器                             │
│  vite.config.ts                                          │
│    ├── /api  → Netease API (动态端口)                     │
│    ├── /unblock-api → Unblock Match (动态端口)            │
│    └── /dl-proxy → 音频 CORS 代理                         │
└─────────────────────────────────────────────────────────┘
```

---

## 文件清单

| 文件 | 角色 |
|------|------|
| `electron/port-manager.js` | 端口可用性检测与冲突解决 |
| `electron/serviceManager.js` | 后端服务的启动/健康检查/终止（API + Proxy） |
| `electron/unblock-native-match.js` | 内置 match 能力（桌面端首选路径，IPC 调用） |
| `electron/main.js` | Electron 主进程，编排启动流程 + IPC handler |
| `electron/preload.js` | 解析 `--service-ports` 参数，暴露 `appEnv` + `unblockBridge` |
| `scripts/start-desktop.mjs` | 开发模式总控脚本，启动所有服务（含 match fallback） |
| `vite.config.ts` | 从环境变量读取动态代理目标 |
| `server/unblock-match-server.mjs` | 独立 match HTTP 服务（Web 端主路径 / 桌面端 fallback） |

---

## 各模块详情

### 1. `electron/port-manager.js` — 端口探测

**核心函数：**

- `isPortAvailable(port, host?)` — 用 `net.createServer().listen()` 测试端口是否可用，返回 `boolean`
- `pickPort(basePort, maxTries?)` — 从 basePort 开始递增查找可用端口
- `resolveServicePorts(preferred?, maxTries?)` — 依次解析 4 个服务端口，保证无冲突

**冲突解决策略：**

1. 从基准端口开始，逐 +1 探测可用端口（最多试 20 次）
2. Vite 优先探测（需要已知端口做 wait-on）
3. 已分配端口会被加入 `usedPorts` Set，后续服务跳过

```js
// 使用示例
import { resolveServicePorts } from './port-manager.js';
const ports = await resolveServicePorts();
// → { vite: 5173, api: 38761, unblockProxy: 38762, unblockMatch: 38763 }
```

### 2. `electron/serviceManager.js` — 服务管理

**能力：**

- `startAllServices(ports, skipUnblock?)` — 启动 Netease API 和 Unblock Proxy
- `waitApiReady(apiBaseUrl, timeoutMs?)` — 轮询 API `/` 端点，等待就绪
- `killAllServices(children)` — 终止所有子进程（SIGTERM）

**Match 服务说明：**
Match 能力已收敛到 Electron 主进程内部（`unblock-native-match.js`），通过 IPC 调用。
外部 match 子进程不再由 `serviceManager.js` 启动，改由 `start-desktop.mjs` 在开发模式下启动作为 fallback。
Web 端仍通过 `npm run dev:unblock-match` 独立启动。

**启动的服务：**

| 服务 | 启动命令 | 说明 |
|------|----------|------|
| Netease API | `node -e <bootstrap>` （`ELECTRON_RUN_AS_NODE=1`） | 通过 bootstrap 从包内真实 package root `require()`，避免打包后依赖丢失 |
| Unblock Proxy | `node -e <bootstrap>` （`ELECTRON_RUN_AS_NODE=1`） | 通过 bootstrap 从 `@unblockneteasemusic/server` 包根启动，兼容 `precompiled` 资源 |

> Match 服务已收敛到 Electron 主进程内部（`unblock-native-match.js`），不再通过 serviceManager 启动外部子进程。
> Web 端仍通过 `npm run dev:unblock-match` 独立启动。

**Netease API 入口点解析逻辑：**

1. 优先查找打包后的 `app.asar/node_modules/@neteasecloudmusicapienhanced/api/`
2. 若未命中，再回退到 `app.asar.unpacked/node_modules/@neteasecloudmusicapienhanced/api/`
3. 最后回退到开发目录的 `node_modules/@neteasecloudmusicapienhanced/api/`
4. 只有在 package root 无法直接解析时，才尝试 pnpm 实际落盘路径

### 3. `electron/main.js` — 主进程集成

**启动流程：**

```
main.js
  │
  ├── 检查 SERVICE_PORTS 环境变量
  │     ├── 有 → 使用外部已探测端口（开发模式）
  │     └── 无 → 调用 port-manager 自动探测（生产打包模式）
  │
  ├── startAllServices(ports, isDev)
  │     ├── 开发模式：只启动 Netease API（unblock 由 orchestrator 负责）
  │     └── 生产模式：启动全部 3 个服务
  │
  ├── waitApiReady(apiBaseUrl, timeout)
  │     ├── 成功 → 创建主窗口
  │     └── 失败 → 显示错误页面
  │
  └── createMainWindow(ports)
        └── 把端口通过 --service-ports 参数传给 preload
```

**端口传递链路：**

```
resolveServicePorts()
    │
    ▼
startAllServices(ports)     ← 传环境变量给子进程
    │
    ▼
createMainWindow(ports)
    │
    ├── additionalArguments: ['--service-ports={...}']
    ▼
preload.js
    │
    ├── process.argv 解析 --service-ports
    ▼
contextBridge.exposeInMainWorld('appEnv', {
  apiPort, apiBaseUrl, unblockProxyUrl, unblockMatchUrl
})
    │
    ▼
src/api/client.ts
  → window.appEnv.apiBaseUrl      （API 请求地址）
  → window.appEnv.unblockProxyUrl （传递给 match 的代理地址）
```

### 4. `electron/preload.js` — 端口注入

从 `process.argv` 中解析 `--service-ports` JSON，通过 `contextBridge` 安全暴露给渲染进程：

```js
// 渲染进程访问
window.appEnv.apiPort          // Netease API 端口
window.appEnv.apiBaseUrl       // http://127.0.0.1:38761
window.appEnv.unblockProxyUrl  // http://127.0.0.1:38762
window.appEnv.unblockMatchUrl  // http://127.0.0.1:38763
```

### 5. `scripts/start-desktop.mjs` — 开发总控

**职责：**

1. 先尝试清理固定端口 `5173 / 38761 / 38762 / 38763` 上的残留进程
2. 再调用 `resolveServicePorts()` 探测端口
3. 构造各个子进程的环境变量（`VITE_*`、`PORT`、`SERVICE_PORTS`）
4. 依次启动 Vite、unblock-proxy、unblock-match
5. 3 秒后启动 Electron
6. 注册 SIGINT/SIGTERM 清理回调

**当前实现特点：**

- 开发模式不是“纯粹检测到占用就立即切换端口”
- 现状是：**优先尝试回收默认端口**，回收后再走探测
- 只有在默认端口仍不可用，或多个服务之间需要避让时，才会分配到其他端口
- 因此，开发模式与打包版的端口策略并不完全相同

**环境变量传递：**

```
SERVICE_PORTS → JSON.stringify({api, unblockProxy, unblockMatch})
  │
  ├── Vite 环境:
  │     VITE_API_PROXY_TARGET     = http://127.0.0.1:{api}
  │     VITE_UNBLOCK_MATCH_TARGET = http://127.0.0.1:{unblockMatch}
  │     VITE_DEV_SERVER_URL       = http://localhost:{vite}
  │
  ├── Electron 环境:
  │     VITE_DEV_SERVER_URL       = http://localhost:{vite}
  │
  ├── Unblock Match 环境:
  │     PORT             = {unblockMatch}
  │     UNBLOCK_SOURCES  = bodian,kugou,migu,qq,bilibili
  │     (注意：不设置 UNBLOCK_PROXY_URL，直连 HTTPS 避免 CONNECT 问题)
  │
  └── Unblock Proxy 环境:
        ENABLE_FLAC = true
```

### 6. `vite.config.ts` — 动态代理

从环境变量读取代理目标，启动时由 `start-desktop.mjs` 注入：

```ts
const proxyTarget = env.VITE_API_PROXY_TARGET || 'http://127.0.0.1:38761';
const unblockMatchTarget = env.VITE_UNBLOCK_MATCH_TARGET || 'http://127.0.0.1:38763';
```

代理规则：

| 路径 | 目标 |
|------|------|
| `/api/*` | `{VITE_API_PROXY_TARGET}`（Netease API） |
| `/unblock-api/*` | `{VITE_UNBLOCK_MATCH_TARGET}`（Match Server） |
| `/dl-proxy?url=...` | Vite 中间件，透传音频流（添加 CORS 头） |

### 7. `server/unblock-match-server.mjs` — 端口支持

通过环境变量 `PORT` 指定监听端口（原为固定 38763）：

```js
const PORT = Number(process.env.PORT) || 38763;
server.listen(PORT, '0.0.0.0', ...);
```

---

## 开发模式与生产模式

### 开发模式 (`npm run dev:desktop`)

```
start-desktop.mjs  ← 总控脚本
  ├── 先清理固定端口上的残留进程
  ├── 探测端口
  ├── 启动 vite / unblock-proxy / unblock-match(fallback)
  ├── 设置 SERVICE_PORTS 环境变量
  └── 启动 Electron
        └── main.js 检测到 SERVICE_PORTS → 跳过端口探测，直接使用
              └── startAllServices(ports, skipUnblock=true)
                    只启动 Netease API，不启动 unblock proxy（已由 orchestrator 启动）
              └── initNativeUnblockMatch()
                    主进程内嵌 match 能力（native bridge，首选路径）
              └── 渲染进程 IPC 调用 native bridge
                    └─ 无结果时 fall through 到 HTTP :38763/match（fallback）
```

补充说明：

- 开发模式的目标是尽量把服务拉回默认端口，方便本地调试和复现
- 所以当前 `scripts/start-desktop.mjs` 会主动尝试结束占用这些默认端口的进程
- 这一步属于开发期便利逻辑，不代表生产打包版行为
- 如果默认端口未能回收，后续仍会自动切换到新的可用端口，并通过 `SERVICE_PORTS` / `--service-ports` 传递给 Electron 与渲染层

### 生产模式（打包后）

```
用户启动 Electron
  └── main.js SERVICE_PORTS 不存在 → 自动探测端口
        └── startAllServices(ports, skipUnblock=false)
              启动 Netease API + Unblock Proxy
        └── initNativeUnblockMatch()
              主进程内嵌 match 能力（native bridge，唯一 match 路径）
              生产模式不启动外部 match 服务，native bridge 是唯一路径
```

与开发模式不同，打包版不会先尝试清理固定端口占用，而是直接基于运行时环境执行端口探测并选取可用端口。

### 打包版补充说明

- mac 打包版主进程已经验证可正确拉起 `Netease API / Unblock Proxy`，Match 由主进程内置桥处理
- API 就绪判定使用 `GET /`，不再使用依赖远端网易接口状态的 `/banner`
- 这样可以避免“本地 API 已启动，但因为上游接口瞬时异常而被误判为未就绪”的假失败

### mac 本地构建注意事项

- electron-builder 在本机无 Developer ID 证书时会跳过正式签名
- 当前项目本地构建脚本会在 `dist-build` 输出后自动执行：
  - `xattr -cr <App>.app`
  - `codesign --force --deep --sign - --timestamp=none <App>.app`
  - `codesign --verify --deep --strict <App>.app`
- 这是为了避免产出签名状态异常的 `.app`，导致 Finder 启动前就崩溃

---

## 常见问题

### Q: 端口探测失败

确保没有其他进程占用了基准端口 ~ 基准端口+20 的范围。默认基准端口：

| 服务 | 基准端口 |
|------|----------|
| Vite | 5173 |
| API | 38761 |
| Unblock Proxy | 38762 |
| Unblock Match | 38763 |

### Q: 为什么开发模式看起来总是优先使用默认端口？

因为 `scripts/start-desktop.mjs` 当前会先尝试清理默认端口上的残留进程，再执行端口探测。

所以开发模式实际策略是：

1. 优先回收默认端口
2. 回收失败时再自动切换

如果你机器上有其他长期运行的本地服务刚好占用了这些端口，开发脚本可能会尝试结束它们；这属于当前开发脚本行为，不是打包版行为。

### Q: 为什么 match server 不设 UNBLOCK_PROXY_URL？

`@unblockneteasemusic/server` 的匹配函数在 `global.proxy` 被设置时，所有 HTTPS 请求会通过该代理的 CONNECT 隧道。本地 unblock proxy 的 CONNECT 隧道对 `music.163.com` 的 TLS 握手失败（`SSL_ERROR_SYSCALL`），导致匹配直接挂掉。因此 match server 直连 HTTPS，不经过 unblock proxy。

### Q: 为什么 fee 探测不加 proxy 参数？

同上，API 服务器若配置了 `proxy` 参数，其对 `music.163.com` 的 HTTPS 请求会通过 unblock proxy 的 CONNECT 隧道，TLS 握手失败导致探测异常。Fee 探测直连判断官方可播性，音源替换由独立的 match server 负责。
