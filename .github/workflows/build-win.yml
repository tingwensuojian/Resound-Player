name: Build Windows App\n\non:\n  workflow_dispatch:\n\njobs:\n  build-win:\n    runs-on: windows-latest\n    permissions:\n      contents: write\n    steps:\n      - uses: actions/checkout@v5\n      - uses: actions/setup-node@v5\n        with:\n          node-version: 24\n      - name: Install deps\n        shell: pwsh\n        run: npm install\n      - run: npm run dist:win\n      - uses: actions/upload-artifact@v7\n        if: always()\n        with:\n          name: Resound-Player-Win-Setup\n          path: dist-build/*.exe\n          if-no-files-found: warn\n          retention-days: 14\n